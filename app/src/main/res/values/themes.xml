<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dark window background so no white flash shows before Compose draws. -->
    <style name="Theme.BabyTracker" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/night</item>
    </style>
</resources>
